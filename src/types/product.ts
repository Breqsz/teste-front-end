// Espelha um item do array `products` no JSON da API (nomes dos campos iguais ao backend)
export interface Product {
  productName: string
  descriptionShort: string
  photo: string
  price: number
}
